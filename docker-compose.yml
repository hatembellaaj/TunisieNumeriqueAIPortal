version: "3.9"

services:
  backend:
    build: ./backend
    container_name: whisper_backend
    ports:
      - "15610:5610"
    volumes:
      - ./backend/temp_audio_input:/app/temp_audio_input
    restart: always

  frontend:
    build: ./frontend
    container_name: whisper_frontend
    ports:
      - "18887:80"
    depends_on:
      - backend
